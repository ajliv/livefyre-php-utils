=== 1.1.4 2014-05-07

* Added rating, counting as acceptable types. Removed stream_type.

* Renaming stream to type and adding checks to make sure that type is of a recognized type. Adding legacy stream_type support.

* Fixed validateLivefyreToken and adjusted collectionMetaToken to allow IDNs.

* Moved build checksum into its own method.

* Added getCollectionId method and added checksum to collection meta token.

* Initial commit.